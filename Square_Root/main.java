package Square_Root;

public class main {
    public static void main(String[] args) {
        // SquareRoot squareRoot = new SquareRoot();
        BetterSolution betterSolution = new BetterSolution();
        // squareRoot.mySqrt(2);
        betterSolution.mySqrt(4);
    }
}
